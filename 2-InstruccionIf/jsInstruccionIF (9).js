function Mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
	
var num;
//num = Math.floor (Math.random() * (10 - 1)) + 1;
num=Math.round(Math.random()*(10-1)+parseInt(1));

//Math.round(variableNumero) redondea cualquier numero ingresado
var num2=8.9;
num2=Math.round(num2);

alert (num);
}//FIN DE LA FUNCIÓN